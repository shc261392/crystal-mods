# ── Crystal Mods workspace ────────────────────────────────────────────────────
# Agent-managed scratch space (per copilot-instructions). NEVER commit.
.copilot_workspace/

# ── OS / IDE ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/
*.swp
*.swo
*~

# ── Secrets / env ────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# ── Python (uv) ──────────────────────────────────────────────────────────────
.venv/
.venv-*/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
build/

# ── Node (Volta + pnpm) ──────────────────────────────────────────────────────
node_modules/
.pnpm-store/
.turbo/
.next/
.nuxt/
.cache/
dist/
build/
build.*/

# ── Binary game files (NEVER COMMIT - not mergeable, breaks git best practices)
# Build these from source using Makefiles when needed. Store built assets separately.
*.sga
*.ucs
*.pak
*.bin
*.assets
*.unity3d
*.assetbundle
*.bundle

# Font files - download from official sources instead
*.ttf
*.ttc
*.otf
hybrid_fonts_tc/

out/

# ── Game / mod artifacts ─────────────────────────────────────────────────────
# Binary archive formats (rebuild from source)
*.sga
*.sga.disabled
*.sga0
*.arciv
*.pak
*.pak.bak
*.uasset.bak

# EXCEPTION: Official release SGA files (v1.0.3 and later)
!dawn-of-war-de/unofficial-tc-patch/EnginLocMod.sga

# Deploy state / backups produced by mod scripts at runtime
backup/
*.bak
*.old
*.tmp

# Build / release artifacts (use GitHub Releases instead)
releases/
*.zip
*.7z
!docs/**/*.zip
!**/fixtures/**/*.zip

# Local AI model artifacts (re-downloadable; not mod source)
*.safetensors
*.pth
*.pt
*.onnx
**/weights/
**/models/

# Extracted game references (re-extract as needed; often copyrighted)
reference/raw/
reference/extracted/

# Per-mod extracted source data (regenerate via scripts/extract_*.py)
**/extracted/

# Per-mod translation project source.jsonl (regenerate via scripts/build_tl_source.py).
# Keep project.json, glossary.tsv, state.jsonl in git (curated work).
**/translation/source.jsonl

# ── Tool caches ──────────────────────────────────────────────────────────────
.tool-versions.local
*.log

# Broad ignore for game-data dump dirs (extracted assets, build payloads, etc.).
# NOTE: the curated Battlesector site data under src/data/ is an explicit
# exception below — it is hand-authored and must stay git-tracked.
data/
!warhammer-40k-battlesector/battlesector-tools/src/data/
warhammer-40k-battlesector/battlesector-tools/public/ability-icons/
*.sga